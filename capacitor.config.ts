import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kisandost.pesticide',
  appName: 'Kisan Dost Pesticide App',
  webDir: 'dist'
};

export default config;
